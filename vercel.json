{
  "framework": "vite",
  "buildCommand": "pnpm -w run build:deploy",
  "outputDirectory": "artifacts/trustkeyper/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
